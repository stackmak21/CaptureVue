# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

# Define an abstract target for shared dependencies
abstract_target 'SharedPods' do
  use_frameworks!
  
  # Shared Pods for both targets
  pod 'SwiftGen', '~> 6.0'
  
  
  # Main App Target
  target 'CaptureVue' do
    # Additional pods specific to the main app can be added here
    
  end
  
end
