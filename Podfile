# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CaptureVue' do
  use_frameworks!
  platform :ios, '16.0'

  # Pods for CaptureVue
  pod 'SwiftGen', '~> 6.0'


end
